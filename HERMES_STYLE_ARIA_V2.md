# Miss Aria — Agent V2

The agent layer now includes:

- Persistent SQLite memory
- Recent conversation/session memory
- Persistent task history
- Skill discovery and reusable Markdown skills
- Risk-aware confirmation gates
- Existing Telegram tool registry integration
- `/agent skills`
- `/agent tasks`
- `/agent memory`
- `/agent clear`
- `/aria ...` alias
- Safer error handling around agent execution
- Group-management, research, and memory-management starter skills

The Miss Aria character/personality remains the same. The agent is an orchestration layer, not a replacement personality.
